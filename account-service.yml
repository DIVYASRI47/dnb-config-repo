spring:

  datasource:

    username: root

    password: root

    url: jdbc:mysql://localhost:3306/many

    hikari:

      auto-commit: true

  jpa:

    hibernate:

      ddl-auto: update

    show-sql: true

    properties:

      hibernate:

       dialect: org.hibernate.dialect.MySQLDialect
  jackson:

    serialization:

      fail-on-empty-beans: false

server:

  port: 9090

  error:

    include-message: always
